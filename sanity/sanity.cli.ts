import {defineCliConfig} from 'sanity/cli'

export default defineCliConfig({
  api: {
    // projectId: 'cxyqg2ij',
    // dataset: 'sanity'
    projectId: "1s8s88az",
    dataset: "production",
  }
})
